import { SxProps, Theme } from "@mui/material";

const style: { [key: string]: SxProps<Theme> } = {
  mainPageContainer: {
    width: 1,
    height: 1,
    paddingTop: '7rem',
  },

  headerMainContainer: {
    p: '0 1rem',
    color: 'white'
  },
};

export default style;