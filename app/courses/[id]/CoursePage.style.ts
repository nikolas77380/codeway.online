import { SxProps, Theme } from "@mui/material";

const style: { [key: string]: SxProps<Theme> } = {
  mainPageContainer: {
    maxWidth: "1700px",
    m: "auto",
    width: 1,
    height: 1,
    p: "10px",
  },
};

export default style;
